<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:UniGate.Client.ViewModels"
             x:Class="UniGate.Client.Views.HomeView"
             x:DataType="vm:HomeViewModel">

    <StackPanel Margin="30" Spacing="20">
        <StackPanel>
            <TextBlock Text="{Binding Greeting}" FontSize="32" FontWeight="Bold" Foreground="#2c3e50"/>
            <TextBlock Text="{Binding CurrentDate}" FontSize="16" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Separator Margin="0,10" Opacity="0.5"/>

        <TextBlock Text="Thống Kê Hệ Thống" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <Grid ColumnDefinitions="*, *" RowDefinitions="Auto">
            <Border Grid.Column="0" Background="#3498db" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="🏫 Tổng số trường" Foreground="White" Opacity="0.8"/>
                    <TextBlock Text="{Binding TotalUniversities}" Foreground="White" FontSize="36" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#e67e22" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="👥 Sinh viên tham gia" Foreground="White" Opacity="0.8"/>
                    <TextBlock Text="{Binding TotalUsers}" Foreground="White" FontSize="36" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <Border Background="#ecf0f1" CornerRadius="10" Padding="20" Margin="0,20,0,0">
            <TextBlock Text="💡 Mẹo: Truy cập mục 'Tra Cứu Trường' để xem danh sách chi tiết các trường Đại học mới nhất." 
                       TextWrapping="Wrap" FontStyle="Italic"/>
        </Border>

    </StackPanel>
</UserControl>