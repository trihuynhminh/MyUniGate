<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:UniGate.Client.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
        x:Class="UniGate.Client.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="UniGate Desktop App"
        TransparencyLevelHint="AcrylicBlur"
        Background="White"
        WindowStartupLocation="CenterScreen">

    <Grid ColumnDefinitions="Auto, *">
        
        <Border Grid.Column="0" 
                Width="250"
                IsVisible="{Binding IsSidebarVisible}"
                Background="#f0f4f8" 
                BorderBrush="#e1e4e8" 
                BorderThickness="0,0,1,0">
            
            <StackPanel Spacing="5" Margin="15">
                <TextBlock Text="UniGate" FontSize="28" FontWeight="Bold" 
                           Foreground="#2c3e50" Margin="10,0,0,20"/>
                
                <Button Command="{Binding GoToHomeCommand}"
                        Content="🏠 Trang Chủ" 
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        FontWeight="SemiBold"
                        Padding="10"/>

                <Button Command="{Binding GoToUniversityListCommand}" 
                        Content="🏫 Tra Cứu Trường" 
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        FontWeight="SemiBold"
                        Padding="10"/>
                
                <Button Content="📝 Trắc Nghiệm" 
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        FontWeight="SemiBold"
                        Padding="10"/>
                
                <Separator Margin="0,10" Opacity="0.3"/>

                <Button Command="{Binding GoToLoginCommand}"
                        Content="👤 Đăng Nhập / Tài Khoản" 
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Left"
                        Background="Transparent" 
                        FontWeight="SemiBold"
                        Padding="10"/>
                        
            </StackPanel>
        </Border>

        <Border Grid.Column="1">
            <TransitioningContentControl Content="{Binding CurrentPage}" />
        </Border>
    </Grid>
</Window>