<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:UniGate.Client.ViewModels"
             x:Class="UniGate.Client.Views.UniversityListView"
             x:DataType="vm:UniversityListViewModel">
    
    <Grid RowDefinitions="Auto, *" Margin="20">
        <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="Tra Cứu Thông Tin" FontSize="24" FontWeight="Bold"/>
            <Grid ColumnDefinitions="*, Auto">
                <TextBox Grid.Column="0" Watermark="🔍 Nhập tên trường cần tìm..." Height="35"/>
                <Button Grid.Column="1" Content="Tìm kiếm" Margin="10,0,0,0" Background="#3498db" Foreground="White"/>
            </Grid>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#e1e4e8" BorderThickness="1" CornerRadius="5">
            <ListBox ItemsSource="{Binding Universities}" Background="White">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="60, *, Auto" Margin="5">
                            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#ecf0f1">
                                <TextBlock Text="{Binding UniversityCode}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="10"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock Text="{Binding UniversityName}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Province}" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                            <Button Grid.Column="2" Content="Chi tiết" FontSize="12"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>